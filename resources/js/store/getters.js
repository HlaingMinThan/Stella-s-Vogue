export default {
    cart: (state) => state.cart,
    isCartOpen : (state) => state.isCartOpen,
    isRegionModalBoxOpen : (state) => state.isRegionModalBoxOpen,
    nationality : (state) => state.nationality,
    isSideNavOpen:(state) => state.isSideNavOpen,
    verifyCountDown : (state) => state.verifyCountDown,
    emailVerifyTimerInterval : (state) => state.emailVerifyTimerInterval
};
